import { Options } from './types';

export const defaultOptions: Options = {
  config: '',
  pattern: './',
  fix: false,
  tests: false,
  testExtension: '.spec',
  extLogic: '.js,.ts',
  extStyle: '.css,.scss',
};
